/**
 * This file will expose one function for broadcasting the latest state to all connected clients,
 * it will also expose one function for binding the callback to use for receiving input.
 */

function broadcastState(state) {

}

function inputReceiver(callback) {

}

module.exports = {
  broadcastState,
  inputReceiver
};
